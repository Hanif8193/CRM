-r requirements.txt

# ── Kafka (local/Docker only — not supported on Vercel serverless) ────────────
aiokafka==0.10.0

# ── Lead UI & Management API (local only) ────────────────────────────────────
streamlit>=1.35.0
flask>=3.0.0
requests>=2.31.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
